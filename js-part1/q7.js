// Q.7 What’s difference between map & forEach?

// Ans: map and forEach both are loop. but map has an ability to return an array also we can do some operations with it but forEach can iterate only and we can do some operations with it but it can't return an array.